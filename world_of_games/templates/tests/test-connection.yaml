apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "world_of_games.fullname" . }}-test-connection"
  labels:
    {{- include "world_of_games.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "world_of_games.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
