apiVersion: v1
kind: Service
metadata:
  name: {{ include "world_of_games.fullname" . }}
  labels:
    {{- include "world_of_games.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "world_of_games.selectorLabels" . | nindent 4 }}
