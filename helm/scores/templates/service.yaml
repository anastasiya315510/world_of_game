apiVersion: v1
kind: Service
metadata:
  name: world-of-games
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: world-of-games
